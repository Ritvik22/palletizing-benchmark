'use strict';
const $=id=>document.getElementById(id);
const state={user:null,benchmarks:[],benchmark:null,mine:[],revision:null,newRevision:null};
const num=(v,d=3)=>Number.isFinite(v)?v.toLocaleString(undefined,{maximumFractionDigits:d}):'—';
const node=(tag,text,cls)=>{const e=document.createElement(tag);if(text!==undefined)e.textContent=String(text);if(cls)e.className=cls;return e;};
const button=(text,action,cls='quiet')=>{const e=node('button',text,cls);e.type='button';e.onclick=()=>run(action);return e;};
function notify(message,error=false){$('notice').textContent=message;$('notice').className=error?'error':'';$('notice').hidden=false;}
async function api(path,method='GET',data){const r=await fetch(path,{method,headers:data===undefined?{}:{'Content-Type':'application/json'},body:data===undefined?undefined:JSON.stringify(data)});const body=await r.json();if(!r.ok)throw new Error(typeof body.detail==='string'?body.detail:JSON.stringify(body.detail));return body;}
async function run(action){try{return await action();}catch(e){notify(e.message,true);const open=document.querySelector('dialog[open]');if(open){let p=open.querySelector('.dialog-error');if(!p){p=node('p','', 'dialog-error bad');p.setAttribute('role','alert');open.append(p);}p.textContent=e.message;}}}
function show(id){const d=$(id);d.querySelectorAll('.dialog-error').forEach(e=>e.remove());if(!d.open)d.showModal();}
document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>b.closest('dialog').close());
function requireUser(){if(state.user)return true;show('auth-dialog');return false;}
function bindForm(id,action){$(id).onsubmit=e=>{e.preventDefault();const b=$(id).querySelector('button[type="submit"]');b.disabled=true;run(action).finally(()=>b.disabled=false);};}
function downloadJSON(value,name){const url=URL.createObjectURL(new Blob([JSON.stringify(value,null,2)],{type:'application/json'}));const a=node('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
async function account(){const [me,options]=await Promise.all([api('/api/auth/me'),api('/api/auth/options')]);state.user=me.user;$('account-name').textContent=me.user?.name||'';['tokens','logout','workspace'].forEach(id=>$(id).hidden=!me.user);$('signin').hidden=!!me.user;$('google-signin').hidden=!options.google;$('email-send').disabled=!options.email;$('email').disabled=!options.email;$('local-admin-form').hidden=!options.local_admin;$('auth-status').textContent=options.configured?'Sign in to manage your strategies and submissions.':options.local_admin?'Google and email are not configured. Use the local preview login below.':'Researcher sign-in is not enabled on this installation yet. The owner must configure Google or email delivery.';if(me.user)await mine();}
async function mine(){state.mine=await api('/api/mine');$('my-strategies').replaceChildren();if(!state.mine.length)$('my-strategies').append(node('p','You have no strategies yet. Create one to start a draft.'));for(const s of state.mine){const row=node('div',undefined,'strategy'),head=node('div',undefined,'strategy-heading');head.append(node('strong',s.name),button('New revision',()=>newRevision(s.id)));row.append(head,node('p',s.description,'note'));const links=node('div',undefined,'revision-links');s.revisions.forEach(r=>links.append(button('r'+r.number+' · '+r.state,()=>detail(r.id))));row.append(links);$('my-strategies').append(row);}}
async function loadBenchmark(){state.benchmark=state.benchmarks.find(b=>b.id===$('benchmark').value);const b=state.benchmark;if(!b)return;$('benchmark-info').replaceChildren(node('strong',num(b.orders,0)+' orders · '+num(b.boxes,0)+' boxes'),node('p',b.container.width+' × '+b.container.depth+' × '+b.container.height+' m','note'));$('rules').textContent='Evaluator: '+b.evaluator_version+' · Whole-pack vertical-load equilibrium, bounds, overlap and identity checks. Stacking rule: '+b.stacking_rule+'. Dataset terms: '+b.license+'.';$('download').href='/api/benchmarks/'+encodeURIComponent(b.id);$('download').download=b.id+'.json';await leaderboard();}
let leaderboardRequest=0;
async function leaderboard(){
 if(!state.benchmark)return;
 const benchmarkId=state.benchmark.id,request=++leaderboardRequest;
 const current=()=>request===leaderboardRequest&&state.benchmark?.id===benchmarkId;
 $('leaderboard').replaceChildren();
 $('leaderboard').setAttribute('aria-busy','true');
 $('empty-results').hidden=true;
 try{
  const entries=await api('/api/benchmarks/'+encodeURIComponent(benchmarkId)+'/leaderboard');
  // A previous selection may finish later; it must not overwrite this benchmark.
  if(!current())return;
  $('empty-results').hidden=entries.length>0;
  for(const e of entries){
   const s=e.summary,r=node('tr'),who=node('td');
   who.append(node('strong',e.name),node('p',e.display_name,'note'));
   r.append(node('td',e.rank?'#'+e.rank:'Unranked'),who,node('td','r'+e.number),node('td',s.orders_complete+' / '+s.orders_expected),node('td',s.orders_missing+' / '+s.orders_invalid),node('td',num(s.lve_complete_mean)+' (n='+s.lve_n+')'),node('td',new Date(e.published).toLocaleDateString()));
   const act=node('td');act.append(button('View',()=>detail(e.id)));r.append(act);
   $('leaderboard').append(r);
  }
 }catch(error){if(current())throw error;}
 finally{if(current())$('leaderboard').setAttribute('aria-busy','false');}
}
async function newRevision(sid,parent=null,bid=null){if(!requireUser())return;if(!state.benchmark)throw Error('No benchmark has been imported.');state.newRevision={sid,parent,bid:bid||state.benchmark.id};$('revision-context').textContent=(parent?'Inherits packs from '+parent+'. ':'Starts empty. ')+'Benchmark: '+state.newRevision.bid;$('revision-form').reset();show('revision-dialog');}
bindForm('strategy-form',async()=>{const s=await api('/api/strategies','POST',{name:$('strategy-name').value,description:$('strategy-description').value});$('strategy-dialog').close();await mine();await newRevision(s.id);});
bindForm('revision-form',async()=>{const c=state.newRevision;const config=JSON.parse($('revision-config').value);if(!config||Array.isArray(config)||typeof config!=='object')throw Error('Configuration must be a JSON object.');const r=await api('/api/strategies/'+c.sid+'/revisions','POST',{benchmark_id:c.bid,parent_id:c.parent,notes:$('revision-notes').value,code_revision:$('code-revision').value,model_revision:$('model-revision').value,config});$('revision-dialog').close();await mine();await detail(r.id);});
function metric(target,label,value){const e=node('div');e.append(node('strong',value),node('span',label));target.append(e);}
async function detail(id){const r=await api('/api/revisions/'+encodeURIComponent(id));state.revision=r;$('detail-title').textContent=r.strategy_name+' · revision '+r.number+' · '+r.state;const body=$('detail-body');body.replaceChildren();const s=r.summary,metrics=node('div',undefined,'metrics');metric(metrics,'Valid complete orders',s.orders_complete+' / '+s.orders_expected);metric(metrics,'Missing orders',s.orders_missing);metric(metrics,'Invalid orders',s.orders_invalid);metric(metrics,'Mean LVE · complete orders only',num(s.lve_complete_mean));body.append(metrics);
 const meta=node('dl',undefined,'revision-meta');for(const [k,v] of [['Researcher',r.author],['Benchmark',r.benchmark_id],['Algorithm',r.code_revision],['Model',r.model_revision],['Notes',r.notes],['Evaluator',s.evaluator_version],['Parent',r.parent_id||'Initial revision'],['Snapshot SHA-256',r.hash||'Draft — not frozen']])meta.append(node('dt',k),node('dd',v));body.append(meta);const config=node('details');config.append(node('summary','Configuration'),node('pre',JSON.stringify(r.config,null,2)));body.append(config);
 const actions=node('div',undefined,'actions');actions.append(button('Download validation report',()=>downloadJSON(r,'revision-'+r.id+'.json')));if(r.parent_id)actions.append(button('Compare with parent',async()=>{const c=await api('/api/revisions/'+r.parent_id+'/compare/'+r.id);const text='Common completed orders: '+c.matched_complete_orders+'. Mean LVE: '+num(c.left_lve)+' → '+num(c.right_lve)+'. Changed orders: '+c.changed_orders.length+'.';const p=node('p',text,'note');actions.after(p);}));
 if(state.user?.id===r.owner){if(r.state==='draft'){const uploader=node('div',undefined,'upload');uploader.append(node('strong','Upload pack JSON'),node('p','Choose one or more files. Accepted shapes: one pack, an array of packs, or {"packs": [...]}. Each file is limited to 8 MiB.','note'));const input=node('input');input.type='file';input.accept='.json,application/json';input.multiple=true;input.setAttribute('aria-label','Packing JSON files');const upload=button('Validate and save',async()=>{if(!input.files.length)throw Error('Choose at least one JSON file.');upload.disabled=true;try{let packs=[];for(const f of input.files){if(f.size>8*1024*1024)throw Error(f.name+' exceeds 8 MiB.');const p=JSON.parse(await f.text());packs.push(...(Array.isArray(p)?p:Array.isArray(p.packs)?p.packs:[p]));}if(new Set(packs.map(p=>p.order_id)).size!==packs.length)throw Error('Upload contains duplicate order IDs.');for(let i=0;i<packs.length;i+=25){await api('/api/revisions/'+r.id+'/packs','PUT',{packs:packs.slice(i,i+25)});notify('Saved and validated '+Math.min(i+25,packs.length)+' / '+packs.length+' packs.');}await detail(r.id);}finally{upload.disabled=false;}});uploader.append(input,upload);body.append(uploader);actions.append(button('Publish revision',()=>{ $('confirm-title').textContent='Publish this revision?';$('confirm-text').textContent='This makes the packs and metadata public and immutable. '+s.orders_missing+' missing and '+s.orders_invalid+' invalid orders will remain in the benchmark denominator.';$('confirm-action').textContent='Publish immutable revision';$('confirm-action').onclick=()=>run(async()=>{await api('/api/revisions/'+r.id+'/publish','POST');$('confirm-dialog').close();await mine();await leaderboard();await detail(r.id);});show('confirm-dialog');}));}else actions.append(button('Create child revision',()=>{$('detail-dialog').close();return newRevision(r.strategy_id,r.id,r.benchmark_id);}));}
 body.append(actions);const ids=Object.keys(r.reports).sort();if(ids.length){const workspace=node('div',undefined,'pack-workspace'),frame=node('iframe'),side=node('div'),label=node('label','Inspect order'),select=node('select'),report=node('div');frame.title='Submitted packing revision';select.id='detail-order';label.htmlFor=select.id;ids.forEach(oid=>{const opt=node('option',oid+(r.reports[oid].valid?'':' · invalid'));opt.value=oid;select.append(opt);});side.append(label,select,report);workspace.append(frame,side);body.append(workspace);select.onchange=()=>{const oid=select.value,p=r.reports[oid];frame.src='/viz?embed=1&view=ep&benchmark='+encodeURIComponent(r.benchmark_id)+'&revision='+encodeURIComponent(r.id)+'&order='+encodeURIComponent(oid);report.replaceChildren(node('p',(p.valid?'Valid':'Invalid')+' · '+p.accepted_boxes+' / '+p.expected+' boxes','badge'),node('p','Static support: '+p.static_equilibrium,'note'));if(!p.valid)report.append(node('p','Invalid geometry is shown for diagnosis, not as an accepted packing plan.','bad'));const list=node('ul');p.errors.forEach(e=>list.append(node('li',e)));report.append(list,button('Download pack',async()=>downloadJSON(await api('/api/revisions/'+r.id+'/packs/'+encodeURIComponent(oid)),oid+'.json')));};select.onchange();}show('detail-dialog');}
async function tokenList(){const rows=await api('/api/auth/tokens');$('token-list').replaceChildren();for(const t of rows){const row=node('div',undefined,'token-row');row.append(node('span',t.name+' · '+t.scopes.join(', ')),button('Revoke',async()=>{await api('/api/auth/tokens/'+t.id,'DELETE');await tokenList();}));$('token-list').append(row);}}
bindForm('token-form',async()=>{const scopes=['submit'];if($('token-publish').checked)scopes.push('publish');const t=await api('/api/auth/tokens','POST',{name:$('token-name').value,scopes,days:30});$('token-result').hidden=false;$('token-value').value=t.token;await tokenList();});
bindForm('email-form',async()=>{await api('/api/auth/email','POST',{email:$('email').value});$('auth-status').textContent='Check your email. Open the sign-in link in this browser within 10 minutes.';});
bindForm('local-admin-form',async()=>{try{await api('/api/auth/local-admin','POST',{username:$('local-username').value,password:$('local-password').value});$('auth-dialog').close();await account();}finally{$('local-password').value='';}});
$('signin').onclick=()=>show('auth-dialog');$('logout').onclick=()=>run(async()=>{await api('/api/auth/logout','POST');state.user=null;await account();});$('new-strategy').onclick=()=>{if(requireUser())show('strategy-dialog');};$('benchmark').onchange=()=>run(loadBenchmark);$('refresh').onclick=()=>run(leaderboard);
$('tokens').onclick=()=>run(async()=>{$('token-result').hidden=true;$('token-value').value='';$('mcp-config').textContent=JSON.stringify({mcpServers:{palletizing:{command:'palletizing-mcp',env:{PALLETIZING_API_URL:location.origin,PALLETIZING_API_TOKEN:'YOUR_PRIVATE_TOKEN'}}}},null,2);await tokenList();show('tokens-dialog');});
run(async()=>{await account();state.benchmarks=await api('/api/benchmarks');for(const b of state.benchmarks){const opt=node('option',b.name);opt.value=b.id;$('benchmark').append(opt);}if(state.benchmarks.length)await loadBenchmark();else{notify('No benchmark is configured. Import a versioned order set before accepting submissions.');$('download').hidden=true;$('new-strategy').disabled=true;}
 if(location.hash.startsWith('#sign-in=')){const token=location.hash.slice(9);history.replaceState(null,'',location.pathname);$('confirm-title').textContent='Complete email sign-in';$('confirm-text').textContent='Continue with the email address that requested this link?';$('confirm-action').textContent='Sign in';$('confirm-action').onclick=()=>run(async()=>{await api('/api/auth/email/finish','POST',{token});$('confirm-dialog').close();await account();});show('confirm-dialog');}});
